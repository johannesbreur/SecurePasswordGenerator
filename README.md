# SecurePasswordGenerator
A Python Tkinter GUI app that generates strong random passwords with a configurable length (8+), ensures inclusion of letters/digits/symbols, shuffles output to avoid patterns, and supports one-click clipboard copy with user-friendly validation messages.
